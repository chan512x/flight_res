om

CREATE TABLE FBOOKINGS( BID SERIAL PRIMARY KEY, FLIGHT_ID VARCHAR, DEPT_TIME VARCHAR, DEPT_DATE VARCHAR, FRO VARCHAR, TOOOO VARCHAR, TYPE VARCHAR, DURATION VARCHAR, ARRIV_TIME VARCHAR, ARRIV_DATE VARCHAR, AIRLINE VARCHAR, BAG VARCHAR, AI_PRICE NUMERIC(10, 2), EMT_PRICE NUMERIC(10, 2), MF_PRICE NUMERIC(10, 2), PRICE NUMERIC(10, 2), PPRICE NUMERIC(10, 2) ,BEST VARCHAR, FLEX VARCHAR, TSTAMP TIMESTAMP);

ALTER TABLE FBOOKINGS ADD COLUMN TSTAMP TIMESTAMP;
CREATE TABLE UBOOKINGS (UID INTEGER REFERENCES USER_CRED(UID), BID INTEGER REFERENCES FBOOKINGS(BID), PRIMARY KEY(UID,BID));

CREATE TABLE USER_CRED( UID SERIAL PRIMARY KEY, EMAIL_ID VARCHAR, HPASSWORD VARCHAR);